[pytest]
markers =
    timeout: enforce a timeout for a test when using pytest-timeout
    no_memory_stub: disable memory LM stubbing to exercise live inference
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore:.*Config.*deprecated.*:DeprecationWarning:pydantic
